NODE_ENV=development
PORT=4000
DATABASE_URL=postgresql://taskflow:taskflow_dev_pw@localhost:5432/taskflow
JWT_SECRET=change_this_to_a_long_random_string
CLIENT_ORIGIN=http://localhost:5173

# Leave blank locally (uploads go to ./uploads on disk). Set in production to switch to S3.
UPLOADS_BUCKET=
AWS_REGION=us-east-1